# Dockerfile para desarrollo con hot-reload
FROM node:20-alpine

# Información del contenedor
LABEL maintainer="Hell House Team"
LABEL description="Backend API para desarrollo (con hot-reload)"

# Directorio de trabajo
WORKDIR /app

# Copiar archivos de dependencias
COPY package*.json ./

# Instalar TODAS las dependencias (incluyendo devDependencies para nodemon)
RUN npm install

# Exponer el puerto del backend
EXPOSE 3000

# Variables de entorno por defecto
ENV NODE_ENV=development
ENV PORT=3000

# El código fuente se montará como volumen
# Comando por defecto (puede ser sobrescrito en docker-compose)
CMD ["npm", "run", "dev"]


